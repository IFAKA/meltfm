ACESTEP_HOST=http://localhost:8001
ACESTEP_TIMEOUT=600
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.2:3b
OLLAMA_TIMEOUT=90
OUTPUT_DIR=output
DEFAULT_DURATION=60
ACESTEP_MODEL=acemusic/acestep-v15-turbo
# Turbo model: 8 steps (fast, good quality)
# Base/SFT model: 60-100 steps (better detail, needs ACE-Step restart with different weights)
DEFAULT_INFER_STEPS=60
PYTORCH_MPS_HIGH_WATERMARK_RATIO=0.0
WEB_PORT=8888
DEV_MODE=1
