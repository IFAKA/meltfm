#!/usr/bin/env bash
# Run the radio from anywhere — resolves to project root automatically.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR" || exit 1
exec uv run python radio.py "$@"
